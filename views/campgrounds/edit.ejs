<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Campground - Yelp Campgrounds</title>
</head>

<body>
    <h1>Edit Campground</h1>
    <a href="/campgrounds/<%= campground._id %>">Back to Campground</a>
    <form action="/campgrounds/<%= campground._id %>?_method=PUT" method="POST">
        <div>
            <label for="title">Title:</label>
            <input type="text" id="title" name="title" value="<%= campground.title %>" required>
        </div>
        <div>
            <label for="location">Location:</label>
            <input type="text" id="location" name="location" value="<%= campground.location %>">
        </div>
        <div>
            <label for="price">Price:</label>
            <input type="number" id="price" name="price" value="<%= campground.price %>">
        </div>
        <div>
            <label for="description">Description:</label>
            <textarea id="description" name="description"><%= campground.description %></textarea>
        </div>
        <button type="submit">Update Campground</button>
        <button type="reset">Reset</button>
    </form>
</body>

</html>